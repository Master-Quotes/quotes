section
	&.is-flex-centered
		display flex
		flex-direction column
		justify-content center
		align-items center
	&.is-full-height
		height 100%
		overflow hidden
	&.is-grid
		display grid
		grid-template-columns repeat(auto-fill, minmax(300px, 1fr))